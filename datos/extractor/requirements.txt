# Dependencias para Cloud Function Extractor de Clima
# Python 3.11

# Framework para Cloud Functions
functions-framework==3.*

# Cliente de Google Cloud Pub/Sub
google-cloud-pubsub==2.*

# Cliente de Secret Manager para obtener API Key
google-cloud-secret-manager==2.*

# Cliente HTTP para llamadas a APIs
# urllib3 2.x tiene SSL más estricto que causa SSLEOFError desde Cloud Run
# con weather.googleapis.com. Se fija < 2 para mantener comportamiento 1.x.
requests==2.32.*
# urllib3 2.x tiene SSL más estricto que causa SSLEOFError desde Cloud Run
# con weather.googleapis.com. Fijado en <2 para mantener comportamiento 1.x.
urllib3>=1.26.18,<2
# httpx como cliente HTTP principal con TLS 1.2 forzado (ver llamar_weather_api)
httpx==0.27.*
